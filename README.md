`harp server portfolio_harp` command runs the server and serves the site

`harp compile portfolio_harp` builds the production version of the site

This is site is based on HarpJS static website generator.
